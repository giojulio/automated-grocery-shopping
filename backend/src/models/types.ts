export type TAddress = {
    cep: number,
    street: string,
    number: number,
    neighborhood: string,
    city: string,
    state: string
};

export type TProduct = {
    id: number,
    desired_qty: number
};