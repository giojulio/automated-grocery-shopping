import React from 'react';
import { useProtectedPage } from '../../hooks/useProtectedPage';

const OrderPage = () => {
    useProtectedPage();
    
    return (
      <>
      </>
    );
};

export default OrderPage;